import {
  learningModuleId,
  learningPathId,
  lessonId,
  type LearningPathSeed,
  type LessonSeed,
  type ModuleSeed,
  type MODULE_CATEGORIES,
  type MODULE_DIFFICULTIES,
} from "../../src/lib/learning";
import type { Role } from "../../src/lib/enums";

type Category = (typeof MODULE_CATEGORIES)[number];
type ModuleDifficulty = (typeof MODULE_DIFFICULTIES)[number];
type ModuleSpec = {
  slug: string;
  title: string;
  category: Category;
  difficulty?: ModuleDifficulty;
  hours?: number;
  prerequisites?: string[];
};

// This seed-only factory intentionally uses the curriculum noun "module".
// eslint-disable-next-line @next/next/no-assign-module-variable
const module = (
  slug: string,
  title: string,
  category: Category,
  difficulty: ModuleDifficulty = "intermediate",
  prerequisites: string[] = []
): ModuleSpec => ({ slug, title, category, difficulty, prerequisites, hours: difficulty === "advanced" ? 5 : 3 });

const MODULE_SPECS: ModuleSpec[] = [
  // DSA confidence roadmap
  module("arrays-hashmaps-two-pointers", "Arrays, Hashmaps, and Two-Pointers", "dsa", "beginner"),
  module("sliding-window-prefix-sums", "Sliding Window and Prefix Sums", "dsa", "beginner", ["arrays-hashmaps-two-pointers"]),
  module("binary-search", "Binary Search and Boundary Invariants", "dsa", "beginner", ["arrays-hashmaps-two-pointers"]),
  module("linked-lists", "Linked Lists and Pointer Discipline", "dsa", "beginner"),
  module("stacks-queues-heaps", "Stacks, Queues, and Heaps", "dsa", "intermediate", ["arrays-hashmaps-two-pointers"]),
  module("trees-graphs", "Trees and Graphs", "dsa", "intermediate", ["stacks-queues-heaps"]),
  module("tries", "Tries and Prefix Structures", "dsa", "intermediate", ["trees-graphs"]),
  module("backtracking", "Backtracking and Pruned Search", "dsa", "intermediate", ["trees-graphs"]),
  module("greedy", "Greedy Algorithms and Exchange Arguments", "dsa", "intermediate", ["arrays-hashmaps-two-pointers"]),
  module("intervals", "Intervals and Sweep Lines", "dsa", "intermediate", ["greedy"]),
  module("dynamic-programming-basics", "Dynamic Programming Basics", "dsa", "intermediate", ["arrays-hashmaps-two-pointers"]),
  module("dp-grids-strings", "Dynamic Programming on Grids and Strings", "dsa", "advanced", ["dynamic-programming-basics"]),
  module("advanced-graphs", "Advanced Graphs: Shortest Paths and Ordering", "dsa", "advanced", ["trees-graphs"]),
  module("bit-manipulation", "Bit Manipulation", "dsa", "intermediate", ["arrays-hashmaps-two-pointers"]),
  module("math-geometry", "Math and Geometry for Interviews", "dsa", "intermediate", ["arrays-hashmaps-two-pointers"]),
  module("dsa-to-real-systems", "From DSA to Real Systems", "dsa", "advanced", ["trees-graphs", "dynamic-programming-basics"]),

  // DevOps / infrastructure
  module("docker-fundamentals", "Docker Fundamentals", "docker", "beginner"),
  module("dockerfiles-image-layers", "Dockerfiles and Image Layers", "docker", "intermediate", ["docker-fundamentals"]),
  module("docker-compose", "Docker Compose", "docker", "beginner", ["docker-fundamentals"]),
  module("kubernetes-fundamentals", "Kubernetes Fundamentals", "kubernetes", "beginner", ["docker-fundamentals"]),
  module("pods-deployments-services", "Pods, Deployments, and Services", "kubernetes", "beginner", ["kubernetes-fundamentals"]),
  module("kubernetes-configmaps-secrets", "Kubernetes ConfigMaps and Secrets", "kubernetes", "intermediate", ["pods-deployments-services"]),
  module("kubernetes-networking-ingress", "Kubernetes Networking and Ingress", "kubernetes", "advanced", ["pods-deployments-services", "tcp-vs-udp"]),
  module("kubernetes-scheduling-resource-limits", "Kubernetes Scheduling and Resource Limits", "kubernetes", "advanced", ["pods-deployments-services", "scheduling-context-switching"]),

  // Operating systems
  module("processes-vs-threads", "Processes vs Threads", "operating_systems", "beginner"),
  module("threading-synchronization", "Threading and Synchronization", "concurrency", "intermediate", ["processes-vs-threads"]),
  module("mutexes-semaphores-condition-variables", "Mutexes, Semaphores, and Condition Variables", "concurrency", "intermediate", ["threading-synchronization"]),
  module("deadlocks-starvation", "Deadlocks and Starvation", "concurrency", "advanced", ["mutexes-semaphores-condition-variables"]),
  module("virtual-memory-page-tables", "Virtual Memory and Page Tables", "operating_systems", "intermediate", ["processes-vs-threads"]),
  module("scheduling-context-switching", "Scheduling and Context Switching", "operating_systems", "intermediate", ["processes-vs-threads"]),
  module("filesystems-file-descriptors", "Filesystems and File Descriptors", "operating_systems", "intermediate", ["processes-vs-threads"]),

  // Networking
  module("tcp-vs-udp", "TCP vs UDP", "networking", "beginner"),
  module("sockets-connection-lifecycle", "Sockets and Connection Lifecycle", "networking", "intermediate", ["tcp-vs-udp"]),
  module("http-request-lifecycle", "HTTP Request Lifecycle", "backend", "beginner", ["tcp-vs-udp"]),
  module("load-balancing", "Load Balancing", "infrastructure", "intermediate", ["http-request-lifecycle"]),
  module("dns-fundamentals", "DNS Fundamentals", "networking", "beginner"),
  module("tls-basics", "TLS Basics", "networking", "intermediate", ["tcp-vs-udp"]),
  module("websockets-streaming", "WebSockets and Streaming", "networking", "intermediate", ["http-request-lifecycle"]),

  // C++
  module("raii-resource-ownership", "C++ RAII and Resource Ownership", "cpp", "beginner"),
  module("move-semantics", "C++ Move Semantics", "cpp", "intermediate", ["raii-resource-ownership"]),
  module("references-pointers-lifetimes", "C++ References, Pointers, and Lifetimes", "cpp", "intermediate", ["raii-resource-ownership"]),
  module("stl-containers-iterators", "C++ STL Containers and Iterators", "cpp", "intermediate"),
  module("unordered-map-hashing-collisions", "C++ unordered_map Hashing and Collisions", "cpp", "intermediate", ["stl-containers-iterators"]),
  module("cpp-templates-basics", "C++ Templates Basics", "cpp", "intermediate"),
  module("concurrency-in-cpp", "Concurrency in C++", "cpp", "advanced", ["threading-synchronization", "raii-resource-ownership"]),

  // Python
  module("python-mutability-identity", "Python Mutability and Identity", "python", "beginner"),
  module("python-generators-iterators", "Python Generators and Iterators", "python", "intermediate"),
  module("python-decorators-closures", "Python Decorators and Closures", "python", "intermediate"),
  module("python-gil-concurrency", "Python GIL and Concurrency", "python", "advanced", ["threading-synchronization"]),
  module("python-hashing-equality", "Python Hashing and Equality", "python", "intermediate"),
  module("python-memory-object-model", "Python Memory and Object Model", "python", "advanced", ["python-mutability-identity"]),

  // Databases and caching
  module("sql-indexes", "SQL Indexes", "databases", "beginner"),
  module("composite-indexes", "Composite Indexes", "databases", "intermediate", ["sql-indexes"]),
  module("transactions-isolation", "Transactions and Isolation", "databases", "intermediate"),
  module("query-plans", "Query Plans", "databases", "intermediate", ["sql-indexes"]),
  module("n-plus-one-queries", "N+1 Queries", "databases", "beginner"),
  module("cursor-pagination", "Cursor Pagination", "databases", "intermediate", ["sql-indexes"]),
  module("database-deadlocks-idempotency", "Database Deadlocks and Idempotency", "databases", "advanced", ["transactions-isolation"]),
  module("redis-caching", "Redis and Caching", "caching", "beginner"),

  // System design and backend
  module("system-design-interview-framework", "System Design Interview Framework", "system_design", "beginner"),
  module("apis-requirements", "APIs and Requirements", "system_design", "beginner", ["http-request-lifecycle"]),
  module("rate-limiting", "Rate Limiting", "system_design", "intermediate", ["apis-requirements"]),
  module("caching-strategies", "Caching Strategies", "caching", "intermediate", ["redis-caching"]),
  module("queues-workers", "Queues and Workers", "distributed_systems", "intermediate"),
  module("replication-sharding", "Replication and Sharding", "distributed_systems", "advanced", ["transactions-isolation"]),
  module("observability", "Observability", "infrastructure", "intermediate"),
  module("reliability-backpressure", "Reliability and Backpressure", "distributed_systems", "advanced", ["queues-workers", "observability"]),

  // Quant dev
  module("market-data-feeds", "Market Data Feeds", "quant_dev", "intermediate", ["tcp-vs-udp"]),
  module("order-books", "Order Books", "quant_dev", "intermediate", ["market-data-feeds"]),
  module("latency-cache-locality", "Latency and Cache Locality", "quant_dev", "advanced", ["raii-resource-ownership"]),
  module("linux-for-quant-dev", "Linux for Quant Dev", "quant_dev", "intermediate", ["processes-vs-threads"]),
  module("networking-for-quant-dev", "Networking for Quant Dev", "quant_dev", "advanced", ["tcp-vs-udp", "sockets-connection-lifecycle"]),
  module("concurrency-for-quant-dev", "Concurrency for Quant Dev", "quant_dev", "advanced", ["threading-synchronization"]),

  // AI usage
  module("good-ai-usage-principles", "Good AI Usage Principles", "ai_usage", "beginner"),
  module("token-efficient-prompting", "Token-Efficient Prompting", "ai_usage", "beginner", ["good-ai-usage-principles"]),
  module("selecting-repo-context", "Selecting Repo Context", "ai_usage", "beginner", ["good-ai-usage-principles"]),
  module("reviewing-ai-code", "Reviewing AI Code", "ai_usage", "intermediate", ["selecting-repo-context"]),
  module("hallucination-detection", "Hallucination Detection", "ai_usage", "intermediate", ["reviewing-ai-code"]),
  module("testing-verification", "Test-First AI Workflows", "ai_usage", "intermediate", ["reviewing-ai-code"]),
];

type RealLesson = Omit<LessonSeed, "id" | "order">;
const real = (input: RealLesson): RealLesson => input;

const REAL_LESSONS: Record<string, RealLesson[]> = {
  "arrays-hashmaps-two-pointers": [real({ slug: "two-sum-hashmap-pattern", title: "Two Sum as a Hashmap Pattern", lessonType: "pattern", difficulty: "easy", estimatedMinutes: 15, contentMarkdown: "## Core pattern\n\nFor a pair satisfying `a + b = target`, each value reveals the complement to seek. Scan once, check a hashmap before inserting, and keep the invariant that the map contains only earlier values.\n\n## Interview value\n\nThis tests time-space tradeoffs, duplicate handling, and whether you can explain why one pass is correct.\n\n## Common mistakes\n\nInserting before checking can self-match; returning values instead of indices violates many contracts.\n\n## Next step\n\nCompare the hashmap solution with two pointers on sorted input.", keyTakeaways: ["Use complements to replace pair enumeration with lookup.", "State the one-pass invariant before coding."], examples: ["For `[2, 7, 11]` and target 9, value 7 finds the earlier complement 2."], linkedProblemSlugs: ["pair-sum-request-ids", "quadratic-settlement-matcher"], sourceUrls: ["https://docs.python.org/3/tutorial/datastructures.html#dictionaries"], isPlaceholder: false })],
  "binary-search": [
    real({ slug: "binary-search-invariant", title: "Binary Search as an Invariant Game", lessonType: "concept", difficulty: "easy", estimatedMinutes: 15, contentMarkdown: "## The bracket invariant\n\nBinary search maintains a bracket `[lo, hi]` that provably contains the answer if one exists. Each step computes `mid = lo + (hi - lo) // 2`, asks one question about `mid`, and discards the half that cannot contain the answer. The loop ends when the bracket is empty (`lo > hi`) or collapses to one candidate.\n\n## Why interviewers ask it\n\nAlmost everyone can describe binary search; far fewer can write one whose boundaries are correct on the first try. Interviewers watch whether you state the invariant (\"the answer, if present, is always inside `[lo, hi]`\"), whether the bracket shrinks every iteration, and how you handle the empty array and the not-found case.\n\n## Exact match vs boundary\n\nTwo shapes cover most interview uses:\n\n1. **Exact match** — return `mid` when `values[mid] == target`, otherwise discard the impossible half; return a sentinel when the bracket empties.\n2. **First true** — the input looks like `false, false, …, true, true` under some predicate, and you want the first `true`. Keep `hi` on a known-`true` (or out-of-range) position and `lo` on the first still-possible position; loop `while lo < hi` and never skip `mid` itself when it might be the answer.\n\n## Common mistakes\n\nComputing `mid = (lo + hi) // 2` overflows in fixed-width languages (use `lo + (hi - lo) // 2`); writing `lo = mid` in a first-true search loops forever when `hi - lo == 1`; forgetting that an empty array must return the sentinel before any comparison.\n\n## Next step\n\nWork the warmups below, then the walkthrough lesson that turns feasibility questions into first-true searches.", keyTakeaways: ["State the bracket invariant out loud before writing the loop.", "Every iteration must strictly shrink the bracket, or the loop cannot terminate.", "Exact-match and first-true searches use different loop conditions; know both shapes."], examples: ["In `[3, 7, 9, 14]` searching 9: bracket [0,3], mid 1 (7 < 9) → [2,3], mid 2 (9 == 9) → return index 2."], linkedProblemSlugs: ["sorted-build-id-lookup", "first-failing-canary", "error-code-range-scan"], sourceUrls: ["https://docs.python.org/3/library/bisect.html", "https://en.wikipedia.org/wiki/Binary_search_algorithm"], isPlaceholder: false }),
    real({ slug: "binary-search-on-answers", title: "Searching the Answer Space", lessonType: "walkthrough", difficulty: "medium", estimatedMinutes: 20, contentMarkdown: "## From arrays to answers\n\nBinary search does not need an array — it needs a *monotone question*. If \"is capacity `c` enough?\" is false for small `c` and true for every larger `c`, the smallest sufficient capacity is a first-true boundary, and you can binary search candidate answers instead of indices.\n\n## Worked example\n\nSuppose nightly archives of sizes `[6, 3, 5, 5]` GB must be uploaded, in order, within 3 nights, and each night uploads a contiguous run limited by capacity `c`.\n\n1. **Feasibility check:** given `c`, greedily pack archives into the current night until adding one would exceed `c`, then start a new night. Count nights used. This is O(n) and is the *question* we ask about each candidate.\n2. **Bounds:** any workable `c` is at least `max(sizes)` = 6 (the largest single archive must fit) and never needs to exceed `sum(sizes)` = 19 (one night takes everything).\n3. **Search:** try `c = 12` → 2 nights, feasible, so shrink right: bracket [6, 12]. Try `c = 9` → nights {6,3}, {5}, wait — {6,3}=9, {5,5}=10 > 9 → {5}, {5} = 3 nights, feasible → [6, 9]. Try `c = 7` → {6}, {3}, {5}, {5} = 4 nights, infeasible → [8, 9]. Try `c = 8` → {6}, {3,5}, {5} = 3 nights, feasible → answer 8.\n\n## Why interviewers ask it\n\nThis pattern tests whether you can (a) spot monotonicity, (b) write a clean feasibility function, and (c) keep the boundary search correct. It also mirrors real capacity-planning questions.\n\n## Common mistakes\n\nSearching a non-monotone space (the halving argument silently breaks); setting `lo = 0` instead of `max(sizes)` and returning an answer no single item fits into; off-by-one at the feasible/infeasible boundary.\n\n## Next step\n\nSolve the bandwidth planner core problem, then the applied metric-lookup variant that returns to plain sorted arrays in a production setting.", keyTakeaways: ["Binary search generalizes from sorted arrays to any monotone predicate.", "The feasibility check is a separate function; get it right before searching.", "Derive tight lower/upper bounds from the problem, not from zero and infinity."], examples: ["Minimum nightly capacity for sizes [6,3,5,5] within 3 nights is 8: {6}, {3,5}, {5}."], linkedProblemSlugs: ["backup-bandwidth-planner", "stale-metric-lookup"], sourceUrls: ["https://en.wikipedia.org/wiki/Binary_search_algorithm", "https://docs.python.org/3/library/bisect.html"], isPlaceholder: false }),
  ],
  "linked-lists": [
    real({ slug: "linked-list-pointer-discipline", title: "Linked Lists and Pointer Discipline", lessonType: "concept", difficulty: "easy", estimatedMinutes: 15, contentMarkdown: "## What a linked list buys you\n\nA singly linked list is a chain of nodes, each holding a value and a `next` reference. Unlike an array, inserting or removing a node next to one you already hold is O(1) — but reaching a position is O(n), and only forward movement is possible. Interview problems test whether you can rearrange the chain *without losing it*.\n\n## The three-pointer walk\n\nMost pointer surgery uses the same skeleton: `prev` (already-processed part), `curr` (node being examined), and a saved `nxt = curr.next` taken **before** any pointer is overwritten. Reversal, deletion, and splicing are all variations on this walk.\n\n## The dummy head\n\nOperations that might change the first node (delete head, insert before head) get much simpler with a sentinel node placed in front: `dummy.next = head`. You then always operate on `some_node.next`, never on a special-cased `head` variable, and return `dummy.next` at the end.\n\n## Why interviewers ask it\n\nLinked-list questions are short to state and expose exactly how carefully you sequence mutations. Losing the rest of the chain by overwriting `next` too early is the single most common failure, followed by head/tail edge cases.\n\n## Common mistakes\n\nOverwriting `curr.next` before saving it; special-casing the head instead of using a dummy node; forgetting to terminate the new tail with `None`/`null` after a rearrangement; testing only length-3 lists and missing the empty and single-node cases.\n\n## Next step\n\nDo the reversal warmup, then the slow/fast walkthrough lesson.", keyTakeaways: ["Save `next` before you overwrite it — every pointer bug is a variation of violating this.", "A dummy head removes head-mutation special cases.", "Know the prev/curr/next walk cold; reversal and deletion are the same skeleton."], examples: ["Reversing 1→2→3: after one step prev=1, curr=2; after two steps prev=2→1, curr=3; finish with prev=3→2→1."], linkedProblemSlugs: ["reverse-approval-chain", "merge-alert-feeds"], sourceUrls: ["https://en.wikipedia.org/wiki/Linked_list"], isPlaceholder: false }),
    real({ slug: "slow-fast-pointer-walkthrough", title: "Slow and Fast Pointers, Worked Through", lessonType: "walkthrough", difficulty: "easy", estimatedMinutes: 18, contentMarkdown: "## Two speeds, one pass\n\nMany linked-list questions that look like they need the length first — middle node, kth from the end — fall to two pointers moving at different speeds or offsets in a single pass.\n\n## Worked example: middle node\n\nWalk `slow` one step and `fast` two steps per iteration over 1→2→3→4→5. When `fast` runs off the end (`fast is None or fast.next is None`), `slow` sits on 3 — the middle. For an even-length list 1→2→3→4, this loop leaves `slow` on 3, the *second* of the two middles; check which middle the contract wants before coding.\n\n## Worked example: kth from the end\n\nAdvance a `lead` pointer k steps from a dummy head, then move `lead` and `trail` together until `lead` reaches the last node. `trail` now sits just **before** the target, which is exactly where you need to be to unlink it: `trail.next = trail.next.next`. The dummy head makes \"remove the first node\" fall out of the same code path.\n\n## Why interviewers ask it\n\nOffset pointers test whether you can reason about distances between references instead of simulating with extra memory. The follow-up \"do it in one pass\" checks that you know traversal itself is the expensive part.\n\n## Common mistakes\n\nStopping the offset walk one node too early or late (walk it by hand on a 2-node list); moving `fast` two steps without checking `fast.next` first; returning the node *at* the position when the unlink needs the node *before* it.\n\n## Next step\n\nDo the middle-of-queue warmup, then the checkpoint-removal core problem; the merge challenge combines chains with a heap.", keyTakeaways: ["Slow/fast pointers find the middle in one pass; know which middle you return for even lengths.", "For deletions, park the trailing pointer one node before the target.", "Hand-simulate on lists of length 0, 1, and 2 before trusting the loop."], examples: ["kth-from-end with k=2 on 1→2→3→4: lead ends on 4, trail on 2, unlink gives 1→2→4."], linkedProblemSlugs: ["middle-of-release-queue", "drop-stale-checkpoint"], sourceUrls: ["https://en.wikipedia.org/wiki/Linked_list", "https://en.wikipedia.org/wiki/Cycle_detection"], isPlaceholder: false }),
  ],
  "dynamic-programming-basics": [
    real({ slug: "dp-state-and-recurrence", title: "DP Is a State Definition, Not a Trick", lessonType: "concept", difficulty: "easy", estimatedMinutes: 20, contentMarkdown: "## Two properties, one discipline\n\nDynamic programming applies when a problem has **optimal substructure** (the best answer for the whole is built from best answers for smaller pieces) and **overlapping subproblems** (the same smaller pieces recur, so caching pays). Recognize those two and the rest is bookkeeping.\n\n## The discipline: define the state in words first\n\nEvery reliable DP solution starts with one written sentence: *\"dp[i] is the ⟨best/count of⟩ ⟨quantity⟩ considering only the first i ⟨items/steps⟩ ⟨with condition⟩.\"* If you cannot say that sentence, you are not ready to write the recurrence. From the sentence, three things follow mechanically: the recurrence (how dp[i] uses earlier entries), the base cases (the smallest i where the sentence is answerable directly), and the answer's location (which entry the question actually asks for).\n\n## Memoization vs tabulation\n\nTop-down memoization is the recursive definition plus a cache — easiest to write, follows the recurrence literally. Bottom-up tabulation fills a table in dependency order — no recursion depth limits and it exposes space optimizations (when dp[i] depends only on dp[i-1] and dp[i-2], two variables replace the whole array).\n\n## Prerequisites\n\nComfort with recursion, arrays, and the arrays/hashing module. Nothing else.\n\n## Why interviewers ask it\n\nDP separates candidates who memorized twenty solutions from those who can derive one. Interviewers listen for the state sentence, the justification of the recurrence, and the base cases — in that order. A correct answer with an unexplained table scores worse than a slower answer with a clean derivation.\n\n## Common mistakes\n\nJumping to code before stating what dp[i] means; base cases that contradict the state sentence (dp[0] for 'first 0 items' should usually be the empty answer, not a special hack); confusing 'count the ways' with 'find the best' recurrences mid-solution.\n\n## Next step\n\nThe walkthrough lesson derives a 1-D DP end to end; then do the two warmups before the core problems.", keyTakeaways: ["State the meaning of dp[i] in one sentence before writing any code.", "Optimal substructure + overlapping subproblems are the two signals that DP applies.", "Recurrence, base cases, and answer location all follow from the state sentence.", "When dp[i] uses only a fixed window of earlier entries, collapse the array to variables."], examples: ["Count-ways state: 'dp[i] = number of distinct ways to reach step i.' Best-value state: 'dp[i] = max revenue using only the first i slots.'"], linkedProblemSlugs: ["release-train-hops", "cheapest-retry-ladder"], sourceUrls: ["https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-spring-2020/pages/lecture-notes/", "https://en.wikipedia.org/wiki/Dynamic_programming"], isPlaceholder: false }),
    real({ slug: "one-dimensional-dp-walkthrough", title: "Deriving a 1-D DP End to End", lessonType: "walkthrough", difficulty: "medium", estimatedMinutes: 22, contentMarkdown: "## The problem\n\nA pipeline has stages with retry costs `[10, 15, 20]`. You may start at stage 0 or 1; from stage i you advance to i+1 or i+2, paying the cost of every stage you land on; you finish once you move past the last stage. Minimize total cost.\n\n## Step 1: the state sentence\n\n\"dp[i] = the minimum total cost to *finish*, starting from stage i.\" (Working backwards; forwards is equally valid.)\n\n## Step 2: recurrence from the sentence\n\nFrom stage i you pay costs[i] and then behave optimally from i+1 or i+2: `dp[i] = costs[i] + min(dp[i+1], dp[i+2])`.\n\n## Step 3: base cases\n\nStages at or past the end cost nothing more: `dp[n] = dp[n+1] = 0`.\n\n## Step 4: answer location\n\nStart at 0 or 1, whichever is cheaper: `min(dp[0], dp[1])`.\n\n## Fill it\n\n`dp[2] = 20 + min(0, 0) = 20`; `dp[1] = 15 + min(20, 0) = 15`; `dp[0] = 10 + min(15, 20) = 25`. Answer `min(25, 15) = 15`: start at stage 1, jump out. Trace one table by hand in every interview — it catches recurrence bugs in seconds.\n\n## Space optimization\n\ndp[i] touches only i+1 and i+2, so two rolling variables replace the array: O(1) space, same recurrence.\n\n## Count-the-ways variant\n\nChange the question to \"how many distinct advance sequences reach past the end\" and only the algebra changes: `ways[i] = ways[i+1] + ways[i+2]`, base `ways[n] = 1`. Min-cost uses min over choices; counting sums over choices. Spotting which combiner the question wants is half the recognition skill.\n\n## Why interviewers ask it\n\nThis derivation sequence — sentence, recurrence, bases, location, hand-trace — is exactly what they want to watch you do on a problem you have not seen.\n\n## Common mistakes\n\nMixing forward and backward definitions mid-derivation; base cases of 0 for a counting problem (the empty completion counts as 1 way, not 0); optimizing space before the table version works.\n\n## Next step\n\nDo release-train-hops and cheapest-retry-ladder, then the non-adjacent revenue and fewest-batches core problems; the grids module extends the same discipline to two dimensions.", keyTakeaways: ["Derive in order: state sentence → recurrence → base cases → answer location → hand-trace.", "min/max problems combine choices with min/max; counting problems combine with +.", "Space-optimize only after the table version is correct."], examples: ["costs [10,15,20]: dp = [25, 15, 20] with dp[3]=dp[4]=0; answer min(dp[0], dp[1]) = 15."], linkedProblemSlugs: ["release-train-hops", "cheapest-retry-ladder", "ad-slot-revenue-plan", "fewest-batches-exact-total"], sourceUrls: ["https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-spring-2020/pages/lecture-notes/"], isPlaceholder: false }),
  ],
  "dp-grids-strings": [
    real({ slug: "grid-dp-tables", title: "DP on Grids: Tables You Can See", lessonType: "pattern", difficulty: "medium", estimatedMinutes: 20, contentMarkdown: "## Two dimensions, same discipline\n\nGrid DP is the 1-D derivation with a two-part state: *\"dp[r][c] = ⟨best/count⟩ for reaching cell (r, c).\"* The recurrence reads straight off the movement rule — if you may enter (r, c) from above or from the left, then `dp[r][c] = grid[r][c] + min(dp[r-1][c], dp[r][c-1])` for cost, or `dp[r][c] = dp[r-1][c] + dp[r][c-1]` for counting.\n\n## Fill order is dependency order\n\nEach cell depends on up and left, so row-by-row, left-to-right fills every dependency first. The first row and first column are the base cases: only one way in, running sums (or 1s, when counting).\n\n## A counting table you can check by eye\n\nRoutes in a 3×3 grid moving only right/down:\n\n```\n1 1 1\n1 2 3\n1 3 6\n```\n\nEvery interior cell is the sum of its up and left neighbors; the corner holds 6 = C(4,2). Hand-filling a 3×3 table is the fastest way to verify a grid recurrence in an interview.\n\n## Space: keep one row\n\ndp[r][c] needs only the current and previous rows, so a single 1-D row updated in place (left-to-right) suffices: `row[c] += row[c-1]` for counting. O(cols) space.\n\n## Prerequisites\n\nThe 1-D DP module — especially the state-sentence discipline; grids only add an index.\n\n## Why interviewers ask it\n\nGrid problems make the DP table visible and testable; they also generalize to real planning problems (cost surfaces, alignment matrices) where the grid is conceptual rather than physical.\n\n## Common mistakes\n\nBase cases that double-count the origin cell; iterating in an order that reads unwritten cells (down-right movement demands top-left fill); off-by-one between grid indices and dp indices when a sentinel row/column is added.\n\n## Next step\n\nDo the robot-routes warmup and the cheapest-cabling core problem, then the string-alignment walkthrough where the 'grid' becomes two strings.", keyTakeaways: ["Grid state = position; recurrence = movement rule; fill order = dependency order.", "First row/column are the base cases — write them before the double loop.", "Verify with a hand-filled 3×3; collapse to one row only after it works."], examples: ["3×3 count table ends at 6; cost table for [[1,3,1],[1,5,1],[4,2,1]] ends at 7 via 1→3→1→1→1."], linkedProblemSlugs: ["warehouse-robot-routes", "cheapest-rack-cabling"], sourceUrls: ["https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-spring-2020/pages/lecture-notes/"], isPlaceholder: false }),
    real({ slug: "string-alignment-walkthrough", title: "Aligning Two Sequences: Edit Distance and LCS", lessonType: "walkthrough", difficulty: "medium", estimatedMinutes: 25, contentMarkdown: "## Strings as a grid\n\nComparing two sequences is grid DP where the axes are prefixes: *\"dp[i][j] = ⟨answer⟩ for the first i items of A versus the first j items of B.\"* Two classics share this shape.\n\n## Edit distance\n\ndp[i][j] = minimum operations (insert, delete, replace) turning A[:i] into B[:j].\n\n- If `A[i-1] == B[j-1]`: the last items align for free — `dp[i][j] = dp[i-1][j-1]`.\n- Otherwise one operation plus the best smaller alignment: `dp[i][j] = 1 + min(dp[i-1][j-1] /*replace*/, dp[i-1][j] /*delete from A*/, dp[i][j-1] /*insert into A*/)`.\n- Bases: `dp[i][0] = i` (delete everything), `dp[0][j] = j` (insert everything).\n\nWorked 2×2 corner for A = [a, b], B = [a, c]: dp[1][1] = 0 (a matches a), dp[2][2] = 1 + min over {dp[1][1]=0, …} = 1 — one replace of b with c.\n\n## Longest common subsequence\n\ndp[i][j] = length of the longest sequence appearing (in order, not necessarily contiguous) in both prefixes.\n\n- Match: `dp[i][j] = dp[i-1][j-1] + 1`.\n- Mismatch: `dp[i][j] = max(dp[i-1][j], dp[i][j-1])` — drop the last item of one side or the other.\n- Bases: first row and column are 0.\n\nLCS is what diff tools compute: the common backbone, with everything off the backbone rendered as insertions and deletions.\n\n## Same grid, different algebra\n\nEdit distance minimizes with a cost of 1 per operation; LCS maximizes matches. Interviewers often pivot from one to the other to test whether you derived or memorized.\n\n## Prerequisites\n\nThe grid-DP lesson; the warmup below (ordered subsequence check) builds the in-order intuition without any table.\n\n## Common mistakes\n\nIndexing dp with string indices instead of prefix lengths (the +1 sentinel row/column exists for the empty prefix); forgetting the free-match case in edit distance; claiming LCS needs contiguity (that is substring, a different recurrence).\n\n## Next step\n\nDo the ordered-log-subsequence warmup, the config-drift challenge (edit distance over config lines), and the shared-history applied problem (LCS as a diff engine).", keyTakeaways: ["Axes are prefix lengths; dp[0][*] and dp[*][0] describe empty prefixes.", "Edit distance: free diagonal on match, 1 + min(three neighbors) otherwise.", "LCS: +1 diagonal on match, max(up, left) otherwise — the algebra is the only difference.", "Diff tools are LCS in production clothing."], examples: ["Edit distance of config lists ['port=80','host=x','debug=on'] → ['port=8080','host=x'] is 2: one replace, one delete."], linkedProblemSlugs: ["ordered-log-subsequence", "config-drift-distance", "shared-history-length"], sourceUrls: ["https://en.wikipedia.org/wiki/Levenshtein_distance", "https://en.wikipedia.org/wiki/Longest_common_subsequence"], isPlaceholder: false }),
  ],
  "sliding-window-prefix-sums": [real({ slug: "sliding-window-when-it-applies", title: "Sliding Window: When It Applies", lessonType: "pattern", difficulty: "easy", estimatedMinutes: 18, contentMarkdown: "## Recognition\n\nSliding windows fit contiguous ranges whose validity can be maintained as boundaries move. Expand right, update compact state, then shrink left until the invariant is restored.\n\n## Interview value\n\nThe pattern tests invariant design rather than memorization.\n\n## Common mistakes\n\nDo not use it for non-contiguous choices, and remember to remove state when the left edge advances.", keyTakeaways: ["Contiguity and a maintainable invariant are the main signals.", "Monotonic boundaries give the usual linear-time proof."], examples: ["A distinct-client window tracks counts while both pointers move only forward."], linkedProblemSlugs: ["longest-unique-session-streak", "clean-request-window", "sliding-window-rate-check"], sourceUrls: ["https://www.techinterviewhandbook.org/algorithms/array/"], isPlaceholder: false })],
  "dockerfiles-image-layers": [real({ slug: "docker-image-layers-why-they-matter", title: "Docker Image Layers and Why They Matter", lessonType: "concept", difficulty: "easy", estimatedMinutes: 18, contentMarkdown: "## Mental model\n\nEach Dockerfile instruction contributes an immutable layer. Reusable earlier layers improve build caching, while frequently changing inputs should usually be copied later. Multi-stage builds keep compilers and intermediate artifacts out of the runtime image.\n\n## Interview value\n\nInterviewers use layer questions to test reproducibility, cache reasoning, image size, and supply-chain awareness.\n\n## Common mistakes\n\nCopying the whole repository before dependency installation invalidates caches; deleting files in a later layer does not erase bytes from earlier layers.", keyTakeaways: ["Order Dockerfile instructions by stability to preserve cache reuse.", "Use multi-stage builds to separate build and runtime contents."], examples: ["Copy lockfiles, install dependencies, then copy changing application source."], linkedProblemSlugs: [], sourceUrls: ["https://docs.docker.com/get-started/docker-concepts/building-images/understanding-image-layers/"], isPlaceholder: false })],
  "pods-deployments-services": [real({ slug: "kubernetes-pods-deployments-services", title: "Kubernetes Pods vs Deployments vs Services", lessonType: "concept", difficulty: "easy", estimatedMinutes: 20, contentMarkdown: "## Three responsibilities\n\nA Pod is a scheduling unit for tightly coupled containers. A Deployment declares and rolls out a desired replica set. A Service provides a stable discovery and traffic endpoint for a changing set of selected Pods.\n\n## Interview value\n\nThe distinction tests whether you can separate workload lifecycle from networking and discovery.\n\n## Common mistakes\n\nDo not treat a Pod as durable, or a Service as the controller that creates replicas.", keyTakeaways: ["Pods run containers; Deployments manage replica rollout; Services provide stable reachability.", "Selectors connect controllers and traffic to matching Pods."], examples: ["A Deployment replaces a failed web Pod while a Service keeps the client endpoint stable."], linkedProblemSlugs: [], sourceUrls: ["https://kubernetes.io/docs/concepts/workloads/pods/", "https://kubernetes.io/docs/concepts/workloads/controllers/deployment/", "https://kubernetes.io/docs/concepts/services-networking/service/"], isPlaceholder: false })],
  "processes-vs-threads": [real({ slug: "processes-vs-threads-interview-model", title: "Processes vs Threads", lessonType: "concept", difficulty: "easy", estimatedMinutes: 18, contentMarkdown: "## Isolation and sharing\n\nProcesses normally have separate virtual address spaces and explicit IPC boundaries. Threads within one process share memory and resources but retain independent stacks and scheduling state.\n\n## Interview value\n\nThis is the foundation for reasoning about isolation, synchronization, context switching, and failure containment.\n\n## Common mistakes\n\nShared memory is not automatically safe, and process isolation is not absolute without operating-system controls.", keyTakeaways: ["Processes emphasize isolation; threads emphasize shared in-process state.", "Sharing reduces communication overhead but creates synchronization obligations."], examples: ["A thread can pass an object reference directly; separate processes generally need IPC or shared-memory coordination."], linkedProblemSlugs: [], sourceUrls: ["https://man7.org/linux/man-pages/man7/pthreads.7.html"], isPlaceholder: false })],
  "threading-synchronization": [
    real({ slug: "data-races-and-critical-sections", title: "Data Races and Critical Sections", lessonType: "concept", difficulty: "easy", estimatedMinutes: 18, contentMarkdown: "## What a data race actually is\n\nA data race is two threads accessing the same memory concurrently, without synchronization, where at least one access is a write. Concurrent *reads* of immutable data are safe and free; the trouble starts the moment one writer joins.\n\n## The innocent-looking line\n\n`counter += 1` is not one operation. It is a load, an add, and a store. Two threads can both load 5, both compute 6, and both store 6 — one increment vanished. No exception is thrown; the program is simply wrong, occasionally, under load, and usually not in the debugger.\n\n## Critical sections\n\nThe fix is mutual exclusion: a *critical section* that only one thread may execute at a time, wide enough to cover the whole read-modify-write. Everything protected by the same lock becomes atomic *with respect to other holders of that lock* — a lock protects data only by convention, so every access path to the shared state must take it.\n\n## Granularity is the real design question\n\nOne big lock is easy to reason about and can serialize everything into a bottleneck; fine-grained locks scale and multiply the ways to deadlock. Interviews usually probe whether you can name what invariant the critical section protects and how far it must extend.\n\n## Why interviewers ask it\n\nRace conditions are the canonical \"works on my machine\" bug. Interviewers check that you can spot the unsynchronized read-modify-write shape on sight, and that you know a race is about *unsynchronized access*, not about code being \"fast\" or \"slow\".\n\n## Common mistakes\n\nBelieving a line of source is atomic because it is short; protecting writes with a lock while reads go bare; assuming language runtimes make compound operations safe (dictionary check-then-insert is a race even in Python).\n\n## Next step\n\nDo the race-classification warmup, then the lost-increment walkthrough.", keyTakeaways: ["Race = concurrent access, same data, no synchronization, at least one write.", "Read-modify-write compounds (x += 1, check-then-insert) are the classic racing shape.", "A critical section must cover the whole invariant, and every access path must take the lock."], examples: ["Two threads run counter += 1 from 5: both load 5, both store 6 — one update lost, no error raised."], linkedProblemSlugs: ["is-it-a-data-race", "shared-cache-dict-race", "shared-counter-undercounts"], sourceUrls: ["https://pages.cs.wisc.edu/~remzi/OSTEP/threads-intro.pdf", "https://man7.org/linux/man-pages/man7/pthreads.7.html"], isPlaceholder: false }),
    real({ slug: "lost-increment-walkthrough", title: "Anatomy of a Lost Increment", lessonType: "walkthrough", difficulty: "medium", estimatedMinutes: 20, contentMarkdown: "## The schedule that loses an update\n\nA request counter starts at 100. Threads A and B each execute `counter += 1`, which compiles to load, add, store:\n\n1. A: load counter → 100\n2. B: load counter → 100 (context switch happened after A's load)\n3. A: store 101\n4. B: store 101 ← A's increment is gone\n\nThe outcome depends on where the scheduler preempts, which is why the bug appears once per million requests in production and never in a unit test.\n\n## Fix 1: a mutex around the compound\n\nAcquire, `counter += 1`, release. Now the load-add-store is one critical section; schedules like the one above are impossible. Cost: every increment serializes on the lock — fine for most counters, a bottleneck for a hot one.\n\n## Fix 2: an atomic increment\n\nHardware compare-and-swap or fetch-and-add (C++ `std::atomic`, Java `AtomicLong`, Go `atomic.AddInt64`) performs the read-modify-write as one indivisible instruction. Cheaper than a mutex for a single variable, but it protects only that variable — the moment the invariant spans two fields (count *and* sum), atomics on each field separately no longer make the *pair* consistent.\n\n## Fix 3: shard the counter\n\nPer-thread or per-CPU counters that a reader sums on demand remove write contention entirely, at the cost of a slightly stale, more expensive read. This is what high-throughput metrics libraries do.\n\n## Why interviewers ask it\n\nThe follow-ups distinguish levels: can you write the losing schedule (junior), choose between mutex and atomic with a reason (mid), and reach for sharding when the lock becomes the bottleneck (senior).\n\n## Common mistakes\n\nDrawing the schedule with the store before the second load (that schedule is fine — you must interleave the loads); using an atomic for one field of a multi-field invariant; adding sleep() calls to \"fix\" the timing.\n\n## Next step\n\nDiagnose the undercounting metrics worker and the torn-average reader, then move to the synchronization toolbox module.", keyTakeaways: ["Write the exact interleaving: both loads must happen before either store.", "Mutex protects arbitrary invariants; an atomic protects exactly one memory word.", "When the lock itself is the bottleneck, shard the state and aggregate on read."], examples: ["Sharded counters: 8 per-thread counters, each incremented locally; the reader sums all 8 for the total."], linkedProblemSlugs: ["shared-counter-undercounts", "metrics-flush-torn-read", "flaky-test-global-state"], sourceUrls: ["https://pages.cs.wisc.edu/~remzi/OSTEP/threads-locks.pdf"], isPlaceholder: false }),
  ],
  "mutexes-semaphores-condition-variables": [
    real({ slug: "mutex-semaphore-condvar-toolbox", title: "The Synchronization Toolbox", lessonType: "concept", difficulty: "medium", estimatedMinutes: 20, contentMarkdown: "## Three tools, three jobs\n\n**Mutex — mutual exclusion.** One holder at a time; the holder must be the one to release. Use it to protect an invariant on shared state. If your sentence is \"only one thread may touch X at a time,\" you want a mutex.\n\n**Semaphore — counting permits.** Initialized to N; acquire decrements (blocking at zero), release increments, and *any* thread may release, not just the acquirer. Use it to limit concurrency (N simultaneous downloads, N pool slots) or to signal availability of N resources. A semaphore does not know what it protects — it just counts.\n\n**Condition variable — waiting for a fact to become true.** Always paired with a mutex and a *predicate* over shared state (\"queue is non-empty\"). A waiter locks the mutex, checks the predicate in a loop, and calls wait, which atomically releases the mutex and sleeps; a signaler changes the state under the same mutex and notifies. The condvar itself stores nothing: notify with no one waiting is a no-op, which is why the predicate — not the notification — carries the truth.\n\n## The while-loop rule\n\nWait in `while not predicate: cv.wait()`, never `if`. Wakeups can be spurious, and another thread may consume the state between the notify and your wakeup. Re-checking the predicate makes both harmless.\n\n## Why interviewers ask it\n\nTool selection reveals understanding faster than implementation: candidates who reach for a semaphore to protect a map, or busy-poll a flag instead of using a condvar, show where their model ends.\n\n## Common mistakes\n\nUsing a binary semaphore as a mutex and losing ownership semantics (any thread can release it); waiting on a condvar without holding its mutex; treating notify as if it queued a message.\n\n## Next step\n\nDo the tool-selection warmup, then the bounded-buffer walkthrough that combines all three ideas.", keyTakeaways: ["Mutex = exclusive access with ownership; semaphore = N permits, no ownership; condvar = sleep until a predicate holds.", "A condvar is always mutex + predicate + while loop; the predicate is the source of truth.", "Choose the primitive from the sentence that describes the requirement."], examples: ["Cap concurrent S3 uploads at 4: semaphore(4). Protect the shared upload registry: mutex. Wake the uploader when work arrives: condition variable."], linkedProblemSlugs: ["pick-the-primitive", "condvar-if-instead-of-while"], sourceUrls: ["https://pages.cs.wisc.edu/~remzi/OSTEP/threads-sema.pdf", "https://man7.org/linux/man-pages/man3/pthread_cond_wait.3p.html"], isPlaceholder: false }),
    real({ slug: "bounded-buffer-walkthrough", title: "Building a Bounded Buffer, Step by Step", lessonType: "walkthrough", difficulty: "medium", estimatedMinutes: 25, contentMarkdown: "## The problem\n\nProducers push items into a queue with capacity 8; consumers pop them. Producers must sleep when the buffer is full, consumers when it is empty — no busy-waiting.\n\n## Step 1: the shared state and its invariant\n\nOne deque plus one mutex. Invariant: 0 <= len(buffer) <= 8, and every access happens under the mutex.\n\n## Step 2: two condition variables\n\n`not_full` (producers wait here) and `not_empty` (consumers wait here), both tied to the same mutex.\n\n```python\ndef put(item):\n    with lock:\n        while len(buf) == CAP:\n            not_full.wait()\n        buf.append(item)\n        not_empty.notify()\n\ndef get():\n    with lock:\n        while not buf:\n            not_empty.wait()\n        item = buf.popleft()\n        not_full.notify()\n        return item\n```\n\n## Why while, not if\n\nTwo consumers wait; a producer adds one item and notifies both directly or via broadcast; the first consumer takes the item; the second wakes to an empty buffer. With `if` it pops from nothing; with `while` it re-checks and sleeps again.\n\n## Why two condvars\n\nWith a single condvar, a consumer's notify can wake another consumer instead of a producer — legal but wasteful, and with notify-one it can *strand* the producer everyone is waiting for. Separate wait-sets deliver wakeups to the side that can make progress.\n\n## Step 3: shutdown\n\nAdd a `closed` flag set under the lock, then `notify_all()` on both condvars. Every wait predicate becomes `while not buf and not closed` (and the full-wait analog); waiters that wake to a closed buffer raise or return a sentinel. Setting the flag without holding the lock, or without the broadcast, leaves sleepers sleeping forever — that is the classic shutdown hang.\n\n## Why interviewers ask it\n\nThe bounded buffer is the smallest structure that exercises mutex + predicate + condvar + wakeup routing + shutdown at once; it is also queue-worker infrastructure in miniature.\n\n## Common mistakes\n\nNotifying before the state change is visible (notify inside the lock *after* mutating is the easy correct order); one condvar with notify-one; forgetting that shutdown must wake *all* waiters.\n\n## Next step\n\nDo the two-condvar core problem and the shutdown-deadlock challenge, then the connection-pool applied problem.", keyTakeaways: ["State + mutex + predicate first; condvars route wakeups, they do not store them.", "Two condvars direct wakeups to the side that can act; while-loops make every wakeup safe.", "Shutdown is part of the design: flag under lock, notify_all, predicates include the flag."], examples: ["Two waiting consumers, one produced item: the notify-all + while-loop pair makes the losing consumer harmlessly re-sleep."], linkedProblemSlugs: ["bounded-queue-two-condvars", "shutdown-deadlock-workers", "connection-pool-semaphore"], sourceUrls: ["https://pages.cs.wisc.edu/~remzi/OSTEP/threads-cv.pdf", "https://docs.python.org/3/library/threading.html#condition-objects"], isPlaceholder: false }),
  ],
  "virtual-memory-page-tables": [real({ slug: "page-tables-virtual-memory", title: "Page Tables and Virtual Memory", lessonType: "concept", difficulty: "medium", estimatedMinutes: 22, contentMarkdown: "## Translation\n\nVirtual memory gives each process an address space whose virtual pages map through page tables to physical frames or to non-resident state. The TLB caches recent translations; a page fault transfers control to the kernel when a mapping is absent or disallowed.\n\n## Interview value\n\nThe topic connects memory isolation, allocation, cache behavior, and performance.\n\n## Common mistakes\n\nA TLB miss is not necessarily a page fault, and a page fault is not always disk I/O.", keyTakeaways: ["Page tables encode virtual-to-physical mappings and permissions.", "The TLB accelerates translation; faults let the kernel resolve missing or invalid access."], examples: ["A copy-on-write write fault can allocate a private frame without reading from disk."], linkedProblemSlugs: [], sourceUrls: ["https://pages.cs.wisc.edu/~remzi/OSTEP/vm-paging.pdf"], isPlaceholder: false })],
  "raii-resource-ownership": [real({ slug: "cpp-raii-in-interviews", title: "C++ RAII in Interviews", lessonType: "concept", difficulty: "easy", estimatedMinutes: 20, contentMarkdown: "## Ownership through lifetime\n\nRAII binds resource acquisition to object construction and cleanup to deterministic destruction. It applies to memory, file descriptors, locks, sockets, and transactions—not only smart pointers.\n\n## Interview value\n\nRAII questions reveal whether code remains correct across early returns, exceptions, moves, and partial construction.\n\n## Common mistakes\n\nA moved-from object must remain valid, and raw ownership without a documented owner invites double release or leaks.", keyTakeaways: ["Express one clear owner and let scope drive cleanup.", "Design move and copy behavior to match the resource semantics."], examples: ["A lock guard releases a mutex on every scope exit, including exceptions."], linkedProblemSlugs: ["cpp-moved-handle-double-close"], sourceUrls: ["https://en.cppreference.com/w/cpp/language/raii.html"], isPlaceholder: false })],
  "sockets-connection-lifecycle": [
    real({ slug: "socket-lifecycle-states", title: "A Connection's Life in Syscalls and States", lessonType: "concept", difficulty: "medium", estimatedMinutes: 20, contentMarkdown: "## Two sides, two API sequences\n\nA TCP server runs `socket() → bind() → listen() → accept()`; a client runs `socket() → connect()`. `connect()` launches the three-way handshake: client sends SYN, server replies SYN-ACK, client answers ACK. The kernel completes handshakes for a listening socket *before* the application calls `accept()` — accept just dequeues an already-established connection from the accept queue. `listen(backlog)` sizes that queue.\n\n## The state machine you will be asked about\n\nEstablishment: `LISTEN → SYN-RECEIVED → ESTABLISHED` (server side); `SYN-SENT → ESTABLISHED` (client side). Teardown is symmetric and four-way: whoever calls `close()` first sends FIN and walks `FIN-WAIT-1 → FIN-WAIT-2 → TIME_WAIT`; the peer enters `CLOSE_WAIT` when the FIN arrives and stays there **until its application also calls close()**, then passes through `LAST-ACK` to `CLOSED`.\n\n## The two states that page you\n\n- **TIME_WAIT** lives on the side that closed *first*, lasts 2×MSL (a minute or so), and is *normal*: it protects against old duplicate segments leaking into a new connection on the same 4-tuple. Thousands of TIME_WAITs on a busy client/proxy are expected — but they consume ephemeral ports.\n- **CLOSE_WAIT** means *your* application received a FIN and never closed its end. CLOSE_WAIT piling up is always an application bug — usually a missing close/release on an error path.\n\n## Why interviewers ask it\n\nMapping API calls to wire events and states shows whether you can debug production symptoms (`ss` output, connection refused, port exhaustion) instead of reciting the handshake as trivia.\n\n## Common mistakes\n\nThinking accept() performs the handshake; treating TIME_WAIT as a leak to be 'fixed'; not knowing which side of the connection holds which teardown state.\n\n## Next step\n\nThe walkthrough lesson follows one request through the states and reads real `ss` output; then do the syscall-mapping warmup.", keyTakeaways: ["accept() dequeues completed handshakes; the kernel establishes connections behind listen().", "TIME_WAIT on the first closer is normal protection; CLOSE_WAIT accumulation is your bug.", "Know the teardown walk: FIN-WAIT-1/2 and TIME_WAIT on one side, CLOSE_WAIT and LAST-ACK on the other."], examples: ["Client closes first: client walks FIN-WAIT-1 → FIN-WAIT-2 → TIME_WAIT; server sits in CLOSE_WAIT until its close(), then LAST-ACK → CLOSED."], linkedProblemSlugs: ["map-syscalls-to-handshake", "close-wait-pileup"], sourceUrls: ["https://www.rfc-editor.org/rfc/rfc9293#section-3.3.2", "https://man7.org/linux/man-pages/man7/tcp.7.html"], isPlaceholder: false }),
    real({ slug: "where-connections-leak", title: "Following One Request Until Something Leaks", lessonType: "walkthrough", difficulty: "medium", estimatedMinutes: 22, contentMarkdown: "## The setup\n\nA Python service calls a payments API over HTTP for each request. Follow one connection's life and see where production problems attach to each stage.\n\n## Stage 1: connect\n\nThe client kernel picks an **ephemeral source port** (a few tens of thousands available, tunable range). The 4-tuple (src IP, src port, dst IP, dst port) names the connection. A proxy that opens a fresh upstream connection per request burns one ephemeral port per request — and gets it back only after TIME_WAIT expires.\n\n## Stage 2: established\n\n`ss -tan` shows `ESTAB`. If the server's accept loop stalls, *new* handshakes still complete into the accept queue until it fills; after that, SYNs are dropped or refused. `ss -ltn` shows the listener's queue as `Recv-Q` (current) vs `Send-Q` (configured backlog) — a Recv-Q pinned at Send-Q is a saturated accept queue.\n\n## Stage 3: teardown, the happy path\n\nWhoever closes first eats TIME_WAIT. Servers that close responses first accumulate TIME_WAIT server-side (usually fine — the *listening* port is not consumed); clients/proxies that close first spend ephemeral ports on it.\n\n## Stage 4: teardown, the leak\n\nThe client library has:\n\n```python\nresp = pool.get_connection().request(payload)\nif resp.status != 200:\n    raise PaymentError(resp.status)   # connection never returned/closed\n```\n\nEvery non-200 leaks one connection object. The server eventually times out and sends FIN; the client kernel ACKs it and parks the socket in **CLOSE_WAIT** — where it stays, because the application still holds the fd and never calls close. `ss -tan state close-wait | wc -l` growing monotonically is this bug's signature. Eventually: fd exhaustion (`Too many open files`) or pool starvation.\n\n## Reading the evidence\n\n- Many TIME_WAIT, stable count: normal churn.\n- CLOSE_WAIT growing: you forgot close() somewhere — find the error path.\n- `Cannot assign requested address` on connect: ephemeral ports exhausted — pool/keep-alive, widen the port range, or spread destination tuples.\n\n## Common mistakes\n\n'Fixing' TIME_WAIT with aggressive reuse settings instead of pooling; hunting the CLOSE_WAIT leak in the kernel instead of in the application's error handling; forgetting that a connection is identified by the 4-tuple, so one destination caps concurrent connections at the ephemeral-port count.\n\n## Next step\n\nDo the close-wait pileup warmup, the backlog core problem, and the port-exhaustion applied problem.", keyTakeaways: ["Ephemeral ports are the client-side budget; per-request connections spend it, TIME_WAIT delays the refund.", "CLOSE_WAIT growth = missing close() in your code, usually on an error path.", "ss Recv-Q/Send-Q on listeners exposes accept-queue saturation before users report timeouts."], examples: ["ss -tan | awk '{print $1}' | sort | uniq -c → 40210 CLOSE-WAIT next to 300 ESTAB is an application leak, not kernel misbehavior."], linkedProblemSlugs: ["close-wait-pileup", "listen-backlog-refused", "ephemeral-port-exhaustion-proxy", "cancelled-request-connection-leak"], sourceUrls: ["https://man7.org/linux/man-pages/man7/tcp.7.html", "https://man7.org/linux/man-pages/man8/ss.8.html"], isPlaceholder: false }),
  ],
  "tcp-vs-udp": [real({ slug: "tcp-vs-udp-backend-quant", title: "TCP vs UDP for Backend and Quant Dev", lessonType: "concept", difficulty: "medium", estimatedMinutes: 20, contentMarkdown: "## Choose transport properties\n\nTCP is an ordered reliable byte stream with congestion control; loss can delay later bytes on that connection. UDP preserves datagram boundaries and supports multicast but leaves loss, reordering, and recovery to the application.\n\n## Interview value\n\nA strong answer connects transport behavior to latency, fanout, correctness, and recovery instead of saying only that UDP is faster.\n\n## Common mistakes\n\nDo not assume UDP cannot be made reliable at the application layer or that TCP orders data across separate connections.", keyTakeaways: ["Transport choice follows correctness, latency, and fanout needs.", "UDP market-data feeds commonly pair sequence detection with a recovery channel."], examples: ["A missing sequence marks an order book stale until replay or snapshot recovery completes."], linkedProblemSlugs: ["multicast-gap-recovery-design", "order-book-sequence-state-machine"], sourceUrls: ["https://www.rfc-editor.org/rfc/rfc9293", "https://www.rfc-editor.org/rfc/rfc768"], isPlaceholder: false }), real({ slug: "tcp-reliability-walkthrough", title: "What TCP Buys You, Traced Packet by Packet", lessonType: "walkthrough", difficulty: "medium", estimatedMinutes: 20, contentMarkdown: "## One lossy transfer, two transports\n\nA sender ships four 1 KB messages A, B, C, D and the packet carrying B is lost. Walk both transports.\n\n## Over TCP\n\nTCP numbers every byte. The receiver gets A (bytes 0–1023), then C and D arrive carrying byte ranges 2048–4095 — the receiver buffers them but **cannot deliver them to the application**, because delivery is an ordered byte stream and bytes 1024–2047 are missing. Duplicate ACKs for byte 1024 (or a retransmission timeout) trigger the sender to retransmit B; once B lands, the receiver releases B, C, D together. The application saw: A, long pause, then B C D in a burst. That pause — retransmission plus in-order delivery — is **head-of-line blocking**, and it is the price of the ordered-reliable contract. On the wire this looks like: `ACK 1024 (dup ×3) → retransmit seq 1024 → ACK 4096`.\n\n## Over UDP\n\nEach message is an independent datagram. A, C, D are delivered to the application the moment they arrive; B is simply gone. Nothing stalls — and nothing recovers B unless *you* built sequence numbers and retransmission into the application. The application saw: A, C, D, and must notice the gap itself.\n\n## The decision this trace teaches\n\nIf later data is useless without earlier data (file contents, database wire protocols), TCP's blocking is exactly what you want. If the newest datum supersedes older ones (market ticks, game state, live telemetry), waiting for a retransmit of stale data is worse than the loss — UDP plus application-level sequencing wins, which is why market-data feeds pair UDP multicast with gap detection and a recovery channel.\n\n## Timing detail interviewers probe\n\nRetransmission needs a trigger: roughly an RTT for duplicate-ACK fast retransmit, or a full retransmission timeout (RTO, commonly starting around 200 ms–1 s) when there are not enough following packets to generate dup-ACKs. Tail latencies quantized near the RTO are a classic signature of loss on a low-traffic TCP connection.\n\n## Common mistakes\n\nSaying 'UDP is faster' without naming what TCP is spending time on (recovery and ordering); assuming TCP loss shows as an error (it shows as latency); forgetting that TCP boundaries are bytes, not messages.\n\n## Next step\n\nDo the transport-selection and datagram-boundary warmups, then the retransmission-latency core problem and the reliable-telemetry challenge.", keyTakeaways: ["TCP converts loss into latency (retransmit + head-of-line blocking); UDP converts loss into gaps.", "Choose by data dependency: ordered streams need TCP; supersedable data prefers UDP + app sequencing.", "p99 spikes quantized near RTO values are the packet-loss signature on TCP."], examples: ["Lost middle segment: receiver holds C, D until B's retransmit lands, then delivers B, C, D in one burst — the app-visible stall is the retransmission gap."], linkedProblemSlugs: ["pick-transport-for-three-services", "udp-message-boundary-bug", "tcp-retransmission-latency-spike", "reliable-udp-telemetry-design"], sourceUrls: ["https://www.rfc-editor.org/rfc/rfc9293", "https://www.rfc-editor.org/rfc/rfc768"], isPlaceholder: false })],
  "rate-limiting": [real({ slug: "what-rate-limiter-does", title: "What a Rate Limiter Actually Does", lessonType: "concept", difficulty: "easy", estimatedMinutes: 18, contentMarkdown: "## Purpose\n\nA rate limiter protects a constrained resource by defining a key, time model, burst policy, storage location, and rejection behavior.\n\n## Interview value\n\nThe topic connects API semantics with distributed state and overload control.\n\n## Common mistakes\n\nAvoid choosing a global key for per-user limits or ignoring atomic updates across servers.", keyTakeaways: ["Define the protected resource and key before choosing an algorithm.", "Burst allowance and sustained rate are separate controls."], examples: ["A token bucket allows a short burst while bounding long-run admission."], linkedProblemSlugs: ["token-bucket-limiter-spec", "sliding-window-rate-check"], sourceUrls: ["https://datatracker.ietf.org/doc/html/rfc6585#section-4"], isPlaceholder: false })],
  "caching-strategies": [real({ slug: "cache-warming-stampedes", title: "Cache Warming and Cache Stampedes", lessonType: "concept", difficulty: "medium", estimatedMinutes: 20, contentMarkdown: "## Separate concerns\n\nWarming preloads likely-hot keys; stampede protection controls concurrent misses. Single-flight, stale-while-revalidate, and bounded refresh concurrency address the latter.\n\n## Interview value\n\nThis tests time-based failure reasoning beyond simply naming Redis.\n\n## Common mistakes\n\nTTL jitter does not solve every hot-key stampede, and locks need expiry and failure handling.", keyTakeaways: ["Warming reduces cold starts; single-flight controls duplicate refresh work.", "Staleness policies need explicit freshness bounds."], examples: ["Thousands of reads during one rebuild can duplicate origin work without coordination."], linkedProblemSlugs: ["cache-write-before-commit", "cache-stampede-product-page"], sourceUrls: ["https://en.wikipedia.org/wiki/Cache_stampede"], isPlaceholder: false })],
  "sql-indexes": [
    real({ slug: "btree-index-mental-model", title: "How a B-Tree Index Finds Your Rows", lessonType: "concept", difficulty: "easy", estimatedMinutes: 18, contentMarkdown: "## What an index actually is\n\nA B-tree index is a separate, sorted structure of (key, row-pointer) entries. Because it is sorted, the database can descend a shallow tree to any key in O(log n) page reads, then follow pointers to the table rows. Without a usable index, the only general plan is a sequential scan of every row.\n\n## What B-trees are good at\n\nEquality (`email = ?`), ranges (`created_at >= ?`), sorted output (`ORDER BY key`), and prefix matches (`LIKE 'ada%'`). All of these exploit the sort order. What they cannot help with: predicates that destroy order, like `LIKE '%son'`, `lower(email) = ?` without a matching expression index, or arithmetic on the column.\n\n## Selectivity decides usefulness\n\nAn index earns its keep when the predicate selects a small fraction of rows. `status = 'pending'` over three status values may match a third of a huge table — the planner will often (correctly) prefer a sequential scan, because random-access row fetches are more expensive per row than a straight scan.\n\n## Indexes are not free\n\nEvery INSERT/UPDATE/DELETE must maintain every index on the table, and each index consumes cache space. Index design is workload design: index what your hot queries filter and sort on, and nothing else.\n\n## Why interviewers ask it\n\nIndex questions test whether you reason from the physical access path — pages read, order exploited, rows fetched — rather than reciting \"indexes make queries fast.\"\n\n## Common mistakes\n\nIndexing every column \"just in case\"; expecting an index to help a leading-wildcard search; ignoring that a function around the column disables a plain index.\n\n## Next step\n\nThe walkthrough lesson reads real query plans before and after adding an index.", keyTakeaways: ["A B-tree serves equality, range, prefix, and ordering predicates by exploiting sort order.", "Low-selectivity predicates make the planner skip the index — often correctly.", "Every index taxes every write; index the workload, not the schema."], examples: ["`WHERE email = 'a@b.c'` on a million-row table: unique B-tree lookup reads a handful of pages instead of the whole table."], linkedProblemSlugs: ["pick-index-for-login-lookup", "wildcard-search-no-index"], sourceUrls: ["https://www.postgresql.org/docs/current/indexes-intro.html", "https://www.sqlite.org/queryplanner.html"], isPlaceholder: false }),
    real({ slug: "reading-explain-before-indexing", title: "Reading EXPLAIN Before You Add an Index", lessonType: "walkthrough", difficulty: "medium", estimatedMinutes: 22, contentMarkdown: "## Plan first, index second\n\nAdding an index without reading the plan is guessing. `EXPLAIN` shows the access path the planner chose; `EXPLAIN ANALYZE` runs the query and shows actual row counts and timing.\n\n## Worked example\n\nQuery: `SELECT id, total FROM orders WHERE customer_id = 42 ORDER BY created_at DESC LIMIT 20` on 20M rows.\n\n1. **Before:** the plan shows `Seq Scan on orders (rows=19,833,102) → Sort → Limit`. Two red flags: the scan visits every row, and the sort processes every match just to keep 20.\n2. **Add** `CREATE INDEX orders_customer_created ON orders (customer_id, created_at DESC)`.\n3. **After:** `Index Scan using orders_customer_created (rows=20)` — the index serves both the equality filter and the ordering, so the LIMIT stops the scan after 20 entries. No sort node at all.\n\n## When the planner ignores your index\n\nCheck these before blaming the planner: a function or cast wraps the column (`lower(email)`), the predicate matches too many rows to beat a sequential scan, the column order in a composite index does not match the query shape, or statistics are stale (`ANALYZE` the table).\n\n## Why interviewers ask it\n\n\"The query is slow, here is the plan\" is a realistic on-call artifact. Strong candidates read the plan's row estimates, name the expensive node, and predict the plan the right index produces.\n\n## Common mistakes\n\nReading only timings and not row counts; verifying with `EXPLAIN` but never `EXPLAIN ANALYZE`, so estimate errors go unnoticed; adding an index that duplicates the leading columns of an existing one.\n\n## Next step\n\nDo the low-selectivity and covering-index core problems, then the index-audit challenge.", keyTakeaways: ["Read the plan's node types and row counts before and after any index change.", "An index that matches filter + order can eliminate both the scan and the sort.", "When the index is ignored, suspect wrapped columns, selectivity, column order, or stale statistics."], examples: ["Composite (customer_id, created_at DESC) turns seq-scan + sort of 20M rows into a 20-row index scan."], linkedProblemSlugs: ["order-status-page-crawl", "covering-index-hot-endpoint", "write-amplification-index-audit"], sourceUrls: ["https://www.postgresql.org/docs/current/using-explain.html", "https://www.sqlite.org/eqp.html"], isPlaceholder: false }),
  ],
  "transactions-isolation": [
    real({ slug: "isolation-levels-anomalies", title: "Isolation Levels and the Anomalies They Allow", lessonType: "concept", difficulty: "medium", estimatedMinutes: 22, contentMarkdown: "## Isolation is a contract about interleavings\n\nTransactions promise atomicity and isolation, but full serializability is expensive, so databases offer levels that permit specific anomalies in exchange for concurrency.\n\n## The anomalies to know by name\n\n- **Dirty read:** seeing another transaction's uncommitted write. Barred at every PostgreSQL level.\n- **Non-repeatable read:** re-reading a row inside one transaction and getting a different committed value.\n- **Phantom:** re-running a predicate query and seeing new matching rows.\n- **Lost update:** two read-modify-write cycles where the second overwrite silently discards the first.\n- **Write skew:** two transactions read the same snapshot, write disjoint rows, and jointly break an invariant no single write violates.\n\n## What the standard levels give you\n\n**Read committed** (PostgreSQL's default): each statement sees data committed before that statement; non-repeatable reads, phantoms, and lost updates are all possible across statements. **Repeatable read / snapshot:** the whole transaction reads one snapshot; lost updates on the same row are detected and abort, but write skew across different rows remains possible. **Serializable:** the system guarantees some serial order, aborting transactions whose dependencies could violate it — application code must retry.\n\n## Why interviewers ask it\n\nMost real payment/inventory bugs are isolation bugs. Interviewers check that you can name the anomaly in a described interleaving and pick the cheapest mechanism that excludes it — not that you always answer \"use serializable.\"\n\n## Common mistakes\n\nBelieving transactions alone prevent lost updates at read committed; calling write skew a lost update; choosing serializable without adding retry handling for aborts.\n\n## Next step\n\nThe walkthrough lesson traces a lost update line by line and repairs it three ways.", keyTakeaways: ["Know the five anomalies by name and the level at which each becomes impossible.", "Read committed re-snapshots per statement; repeatable read snapshots per transaction; serializable adds dependency detection plus retries.", "Pick the cheapest mechanism that excludes the anomaly your invariant fears."], examples: ["Snapshot isolation stops lost updates on one row but permits the classic two-doctors write skew across two rows."], linkedProblemSlugs: ["read-committed-status-check", "double-click-refund", "snapshot-isolation-write-skew-oncall"], sourceUrls: ["https://www.postgresql.org/docs/current/transaction-iso.html", "https://www.postgresql.org/docs/current/mvcc-intro.html"], isPlaceholder: false }),
    real({ slug: "lost-update-walkthrough", title: "Walking Through a Lost Update", lessonType: "walkthrough", difficulty: "medium", estimatedMinutes: 20, contentMarkdown: "## The bug that transactions do not fix\n\nA read-modify-write done as two SQL statements is unsafe at read committed even when wrapped in a transaction.\n\n## Worked interleaving\n\nBalance starts at 100. Session A and session B both run: read balance, compute balance - 30 in the application, write the result.\n\n1. A: `SELECT balance` → 100\n2. B: `SELECT balance` → 100\n3. A: `UPDATE accounts SET balance = 70`; commit\n4. B: `UPDATE accounts SET balance = 70`; commit\n\nTwo 30-unit withdrawals, final balance 70: B's write silently discarded A's. No error was raised anywhere.\n\n## Three repairs and when each fits\n\n1. **Push the arithmetic into the statement:** `UPDATE accounts SET balance = balance - 30 WHERE id = ? AND balance >= 30`, then check the affected-row count. One atomic statement, no window. Best default for counters and balances.\n2. **Pessimistic:** `SELECT … FOR UPDATE` before computing, serializing the two sessions on the row lock. Fits multi-statement logic that genuinely needs the value first; watch lock hold time and ordering.\n3. **Optimistic:** carry a version column: `UPDATE … SET balance = ?, version = version + 1 WHERE id = ? AND version = ?`; zero rows affected means retry. Fits low-contention rows and offline edits.\n\n## Why interviewers ask it\n\nThis interleaving hides in checkout, refund, quota, and inventory code everywhere. Interviewers want the exact schedule that loses the update and a deliberate choice among the three repairs, with tradeoffs.\n\n## Common mistakes\n\nAsserting \"it's in a transaction, so it's safe\"; using FOR UPDATE everywhere and creating lock convoys; optimistic retries without a retry limit or backoff.\n\n## Next step\n\nSolve the double-click refund warmup and the inventory oversell core problem, then the snapshot-consistency applied problem.", keyTakeaways: ["A two-statement read-modify-write loses updates at read committed; the interleaving is four lines long.", "Prefer a single guarded UPDATE; escalate to FOR UPDATE or version checks when logic needs more.", "Every repair has an operational cost: locks serialize, optimistic plans retry."], examples: ["`UPDATE accounts SET balance = balance - 30 WHERE id = 7 AND balance >= 30` returning 0 rows means insufficient funds or a concurrent change — either way, nothing was lost."], linkedProblemSlugs: ["double-click-refund", "inventory-oversell-checkout", "report-snapshot-consistency"], sourceUrls: ["https://www.postgresql.org/docs/current/explicit-locking.html", "https://www.postgresql.org/docs/current/transaction-iso.html"], isPlaceholder: false }),
  ],
  "composite-indexes": [real({ slug: "composite-indexes-query-shape", title: "Composite Indexes and Query Shape", lessonType: "optimization", difficulty: "easy", estimatedMinutes: 20, contentMarkdown: "## Start from the query\n\nA composite index follows predicates and ordering. Equality columns usually lead, followed by range or sort columns.\n\n## Interview value\n\nIndex questions test whether you can explain the visited rows, avoided sort, and write cost.\n\n## Common mistakes\n\nSeparate single-column indexes do not automatically preserve a required composite ordering.", keyTakeaways: ["Index order follows equality, range, and ordering needs.", "Verify choices with an actual query plan."], examples: ["`WHERE customer_id = ? ORDER BY created_at DESC` often benefits from `(customer_id, created_at DESC)`."], linkedProblemSlugs: ["recent-orders-index-warmup", "missing-composite-index-orders"], sourceUrls: ["https://www.postgresql.org/docs/current/indexes-multicolumn.html"], isPlaceholder: false })],
  "apis-requirements": [
    real({ slug: "api-contracts-and-requirements", title: "From Vague Ask to API Contract", lessonType: "concept", difficulty: "easy", estimatedMinutes: 20, contentMarkdown: "## Requirements come in two kinds\n\n**Functional** requirements say what the system does ('partners can query order status'); **non-functional** requirements say how well ('p99 under 200 ms, 99.9% available, reads may be 5 s stale'). Interviews and design docs go wrong when the second kind stays implicit — every hard tradeoff hides in the non-functionals.\n\n## An API is a promise, not a URL\n\nA usable endpoint contract specifies: the resource model (nouns, their IDs, their relationships), the operations and their semantics (safe? idempotent? asynchronous?), inputs with validation rules, outputs including error shapes, and behavioral guarantees (ordering, staleness, rate limits). HTTP gives you vocabulary for much of this — methods with defined safety and idempotency, status codes, headers like Retry-After — use it instead of reinventing it in the body.\n\n## Idempotency is a requirement, not a feature\n\nAny operation a client might retry (which is any operation, once networks are involved) needs a defined answer to 'what happens if this arrives twice?' For naturally idempotent operations (PUT of a full resource) HTTP semantics answer it; for creations and money movement, an idempotency key supplied by the client and stored with the result is the standard contract.\n\n## Unbounded things need bounds\n\nEvery list endpoint needs pagination from day one (the cursor-pagination module is the mechanics); every client-supplied field needs limits; every operation needs a timeout story. Contracts that omit bounds get them retrofitted during an incident.\n\n## Why interviewers ask it\n\nAPI-design questions test whether you can translate ambiguity into precise, checkable promises — the daily work of a backend engineer — and whether you know what HTTP already guarantees versus what your application must add.\n\n## Common mistakes\n\nDesigning URLs before agreeing on requirements; returning 200 with an error string in the body; treating idempotency, pagination, and the error taxonomy as polish instead of contract.\n\n## Next step\n\nThe walkthrough designs one endpoint end to end; then do the contract-critique and status-code warmups.", keyTakeaways: ["Separate functional from non-functional requirements explicitly — the tradeoffs live in the second list.", "A contract = resource model + operation semantics + errors + guarantees, not a URL scheme.", "Define retry behavior (idempotency) and bounds (pagination, limits) for every operation up front."], examples: ["'Partners check order status' becomes: GET /orders/{id} — safe, cacheable for 5 s, 404 vs 410 distinguished, 429 with Retry-After beyond 10 req/s."], linkedProblemSlugs: ["fix-this-api-contract", "choose-the-status-code"], sourceUrls: ["https://www.rfc-editor.org/rfc/rfc9110"], isPlaceholder: false }),
    real({ slug: "designing-one-endpoint-well", title: "Designing One Endpoint Well", lessonType: "walkthrough", difficulty: "medium", estimatedMinutes: 25, contentMarkdown: "## The ask\n\n'Partners need to list their orders.' Design `GET /partners/{partnerId}/orders` properly, decision by decision.\n\n## Decision 1: scope and authorization\n\nThe partner ID is in the path, but the credential decides access: a token scoped to partner A requesting partner B's orders gets **403** (authenticated, not allowed) — or 404 if you choose not to reveal existence. Pick one policy and state it in the contract.\n\n## Decision 2: pagination — cursor, not offset\n\nOrders arrive constantly. `?page=7` shifts under the client as new rows land (the pagination-deletes problem shows the skipped-row bug); `?after=<opaque cursor>&limit=100` pins the position to a stable key like (created_at, id). Keep the cursor opaque so clients cannot parse or forge it, return `next_cursor: null` when exhausted, and cap `limit` at a documented maximum — an uncapped limit is a self-inflicted denial of service.\n\n## Decision 3: filtering and its index contract\n\nAllow `?status=` and `?created_after=`; each documented filter is a promise that an index supports it (the composite-indexes module is the other half of this decision). Filters you document casually become slow queries you cannot remove.\n\n## Decision 4: the response envelope\n\n```json\n{\n  \"data\": [\n    { \"id\": \"ord_9f2k\", \"status\": \"shipped\", \"total_cents\": 1899, \"created_at\": \"2026-07-01T12:03:09Z\" }\n  ],\n  \"next_cursor\": \"eyJjcmVhdGVkX2F0IjoxNzUxMzcxMzg5fQ\"\n}\n```\n\nMoney as integer cents, timestamps in UTC RFC 3339, IDs prefixed and opaque. The `data` wrapper leaves room for metadata later without a breaking change.\n\n## Decision 5: errors and limits\n\n400 for a malformed cursor, 403/404 per the access policy, 429 with `Retry-After` past the rate limit, and one stable machine-readable error shape (`{\"error\": {\"code\": \"invalid_cursor\", \"message\": ...}}`) across all endpoints. Document staleness explicitly ('the list reflects writes within 5 seconds') when reads come from a replica.\n\n## Why interviewers ask it\n\nOne endpoint exercises authorization, pagination, indexing, serialization, and error contracts — five real decisions, each with a wrong default that bites in production.\n\n## Common mistakes\n\nOffset pagination on a moving dataset; floats for money; leaking internal enum values into the contract; unbounded limit; a different error format per endpoint.\n\n## Next step\n\nDo the money-transfer core problem (a mutation with an idempotency contract is harder than any read) and the versioning problem (evolving a contract 200 partners already parse).", keyTakeaways: ["Every list endpoint: opaque cursor pagination, capped limit, documented staleness.", "Each supported filter is an index promise; do not document what you cannot serve.", "Errors are part of the contract: stable codes, correct statuses, Retry-After on 429."], examples: ["A cursor pinned to (created_at, id) survives concurrent inserts that shift every offset-based page."], linkedProblemSlugs: ["money-transfer-api-contract", "api-field-versioning-migration"], sourceUrls: ["https://www.rfc-editor.org/rfc/rfc9110", "https://www.rfc-editor.org/rfc/rfc6585"], isPlaceholder: false }),
  ],
  "system-design-interview-framework": [
    real({ slug: "structure-system-design-answer", title: "How to Structure a System Design Answer", lessonType: "system_design", difficulty: "easy", estimatedMinutes: 22, contentMarkdown: "## Repeatable sequence\n\nClarify users, operations, scale, reliability, and consistency. Define a small API and data model, draw the request flow, deepen bottlenecks, and end with failures, observability, and tradeoffs.\n\n## Interview value\n\nStructure demonstrates prioritization under ambiguity.\n\n## Common mistakes\n\nDo not start capacity math before the workload or name products without explaining the property they provide.", keyTakeaways: ["Requirements determine which tradeoffs matter.", "Reserve time for failures, observability, and recap."], examples: ["Clarify notification channels, delay, ordering, retries, and preferences before selecting queues."], linkedProblemSlugs: ["cache-stampede-product-page", "outbox-publisher-marks-before-send", "clarify-notification-requirements", "envelope-math-image-uploads"], sourceUrls: ["https://sre.google/sre-book/table-of-contents/"], isPlaceholder: false }),
    real({ slug: "webhook-delivery-design-walkthrough", title: "One Design, Start to Finish: Webhook Delivery", lessonType: "system_design", difficulty: "medium", estimatedMinutes: 30, contentMarkdown: "## The prompt\n\n'Design a webhook delivery service: when events happen in our platform, deliver them to customer-registered HTTPS endpoints.' Here is the framework lesson applied for real, timeboxed the way a 45-minute interview goes.\n\n## Minute 0–8: requirements\n\nFunctional: customers register endpoints with event-type filters; events fan out to matching endpoints; customers can replay. Clarifying questions that change the design: *Ordering?* — per-customer best-effort, not global (strict global ordering would serialize everything). *Delivery guarantee?* — at-least-once with retries; customers must dedupe by event ID. *Latency?* — seconds, not milliseconds. Non-functional numbers to negotiate: 10k events/s platform-wide, average fanout 3, so ~30k deliveries/s; customer endpoints are slow (p50 300 ms) and unreliable (5% fail transiently).\n\n## Minute 8–14: API and data model\n\n`POST /webhook-endpoints` (url, event_types[], secret), `GET /deliveries?after=<cursor>` for observability, `POST /deliveries/{id}/replay`. Tables: endpoints, events (immutable payloads), deliveries (event_id, endpoint_id, status, attempt_count, next_attempt_at) — the deliveries row is the unit of work and audit trail.\n\n## Minute 14–25: the flow and the first bottleneck\n\nEvent commits → outbox row in the same transaction (the outbox problem in this module is exactly why) → publisher moves it to a queue → workers claim deliveries, POST with HMAC signature, record outcome. The arithmetic that shapes everything: 30k deliveries/s × 0.3 s mean call time = 9,000 concurrent HTTP calls in flight. That number forces async I/O workers, per-endpoint concurrency caps, and strict timeouts (say 5 s) — one slow customer must not absorb the worker pool (the bulkhead idea from the connection-pool problem).\n\n## Minute 25–35: failures — where the real design lives\n\nRetry with exponential backoff + jitter on 5xx/timeouts (schedule via next_attempt_at, not in-memory sleeps, so restarts lose nothing). A customer endpoint down for hours: per-endpoint circuit breaker and a dead-letter state with replay, so one dead endpoint consumes bounded resources. Duplicate deliveries are contractual (at-least-once): event ID in the payload, customers dedupe; exactly-once to an external HTTP endpoint is not honestly promisable. Poison payloads: cap attempts, park in DLQ, alert the customer, not just yourself.\n\n## Minute 35–40: observability and tradeoffs recap\n\nPer-endpoint success rate, delivery lag histogram, DLQ depth; expose delivery history to customers (halves support load). Recap the two deliberate tradeoffs: at-least-once over exactly-once, and per-customer ordering over global.\n\n## Why this walkthrough matters\n\nNotice where the time went: ~20 of 40 minutes on failure semantics and load arithmetic, ~5 on boxes and arrows. Weak answers invert that ratio.\n\n## Common mistakes\n\nSkipping the 9,000-concurrent-calls computation and proposing synchronous delivery from request threads; promising exactly-once; global ordering 'for safety'; retrying in memory where a deploy loses every pending retry.\n\n## Next step\n\nDo the status-page core scenario with this timebox structure, then the flash-sale challenge where the load math is the whole game.", keyTakeaways: ["Negotiate guarantees early: at-least-once + dedupe and per-key ordering are usually the honest contract.", "One arithmetic line (rate × duration = concurrency) dictates the worker architecture.", "Failure handling — retries with persisted schedules, circuit breakers, DLQ + replay — is most of the design.", "Spend interview minutes proportional to risk: failure semantics over box diagrams."], examples: ["30k deliveries/s × 0.3 s = 9,000 in-flight calls → async workers with per-endpoint caps, not a thread per delivery."], linkedProblemSlugs: ["design-status-page-service", "design-flash-sale-checkout", "outbox-publisher-marks-before-send"], sourceUrls: ["https://sre.google/sre-book/table-of-contents/", "https://www.rfc-editor.org/rfc/rfc9110"], isPlaceholder: false }),
  ],
  "token-efficient-prompting": [real({ slug: "token-efficient-debugging-prompts", title: "Token-Efficient Debugging Prompts", lessonType: "ai_usage", difficulty: "easy", estimatedMinutes: 16, contentMarkdown: "## Relevant context only\n\nState the observed failure, expected behavior, constraints, entrypoint, relevant contract, and validation command. Let the agent search call sites before pasting unrelated files.\n\n## Interview and work value\n\nGood context selection demonstrates system understanding and produces changes that are easier to review.\n\n## Common mistakes\n\nDo not dump secrets or ask for a fix before requiring evidence for the hypothesis.", keyTakeaways: ["Lead with observable behavior and acceptance checks.", "Use progressive disclosure of repository context."], examples: ["Provide the handler, client wrapper, timeout configuration, focused test, and a call-site search."], linkedProblemSlugs: ["ai-debug-prompt-warmup", "ai-token-efficient-repo-task", "ai-select-files-for-timeout-fix"], sourceUrls: ["https://docs.github.com/en/copilot/customizing-copilot/adding-repository-custom-instructions-for-github-copilot"], isPlaceholder: false })],
};

function placeholderLessons(spec: ModuleSpec, startOrder: number): LessonSeed[] {
  const moduleId = learningModuleId(spec.slug);
  return ["foundations", "applied-checkpoint"].map((suffix, index) => ({
    id: lessonId(moduleId, `${spec.slug}-${suffix}`),
    slug: `${spec.slug}-${suffix}`,
    title: `${spec.title}: ${index === 0 ? "Foundations" : "Applied Checkpoint"}`,
    lessonType: index === 0 ? "concept" : "walkthrough",
    difficulty: index === 0 ? "easy" : "medium",
    estimatedMinutes: index === 0 ? 20 : 25,
    contentMarkdown: `> **Content scaffold**\n\nExpand this ${index === 0 ? "instructional" : "interview checkpoint"} for **${spec.title}** with a concise mental model, realistic example, common mistakes, and self-review questions.`,
    keyTakeaways: [`Explain the core interview vocabulary for ${spec.title}.`, "Apply the idea to one realistic scenario."],
    examples: ["Add one small worked example and one boundary case."],
    linkedProblemSlugs: [],
    sourceUrls: [],
    order: startOrder + index,
    isPlaceholder: true,
  }));
}

export const learningModules: ModuleSeed[] = MODULE_SPECS.map((spec) => {
  const moduleId = learningModuleId(spec.slug);
  const authored = (REAL_LESSONS[spec.slug] ?? []).map((lesson, index): LessonSeed => ({
    ...lesson,
    id: lessonId(moduleId, lesson.slug),
    order: index + 1,
  }));
  return {
    id: moduleId,
    slug: spec.slug,
    title: spec.title,
    description: `A standalone interview module covering the mental models, tradeoffs, and practice expected for ${spec.title.toLowerCase()}.`,
    category: spec.category,
    difficulty: spec.difficulty ?? "intermediate",
    estimatedHours: spec.hours ?? 3,
    prerequisites: spec.prerequisites ?? [],
    outcomes: [`Explain the core ideas behind ${spec.title}.`, "Apply the topic in a realistic interview scenario."],
    sourceUrls: authored.flatMap((lesson) => lesson.sourceUrls),
    isPublished: true,
    isPlaceholder: authored.length === 0,
    lessons: [...authored, ...placeholderLessons(spec, authored.length + 1)],
  };
});

type PathSpec = {
  slug: string;
  title: string;
  description: string;
  targetRoles: Role[];
  difficulty: string;
  estimatedHours: number;
  modules: string[];
};

const PATHS: PathSpec[] = [
  { slug: "dsa-confidence-builder", title: "DSA Confidence Builder", description: "Build confidence from easy algorithm fundamentals into core patterns and applied interview variants.", targetRoles: ["new_grad_swe", "backend_swe", "quant_developer", "mid_level_swe"], difficulty: "beginner-to-advanced", estimatedHours: 60, modules: ["arrays-hashmaps-two-pointers", "sliding-window-prefix-sums", "binary-search", "linked-lists", "stacks-queues-heaps", "trees-graphs", "tries", "backtracking", "greedy", "intervals", "dynamic-programming-basics", "dp-grids-strings", "advanced-graphs", "bit-manipulation", "math-geometry", "dsa-to-real-systems"] },
  { slug: "backend-swe", title: "Backend SWE Path", description: "A curated route through HTTP, data, caching, queues, observability, and reliable service design.", targetRoles: ["backend_swe", "mid_level_swe", "fullstack_swe"], difficulty: "beginner-to-advanced", estimatedHours: 34, modules: ["http-request-lifecycle", "apis-requirements", "sql-indexes", "composite-indexes", "transactions-isolation", "redis-caching", "caching-strategies", "queues-workers", "rate-limiting", "observability", "reliability-backpressure"] },
  { slug: "infrastructure-swe", title: "Infrastructure SWE Path", description: "A systems route through processes, memory, networking, containers, Kubernetes, concurrency, and reliability.", targetRoles: ["infrastructure_swe", "platform_engineer", "distributed_systems_engineer"], difficulty: "intermediate-to-advanced", estimatedHours: 40, modules: ["processes-vs-threads", "virtual-memory-page-tables", "tcp-vs-udp", "sockets-connection-lifecycle", "docker-fundamentals", "dockerfiles-image-layers", "kubernetes-fundamentals", "pods-deployments-services", "threading-synchronization", "observability", "reliability-backpressure"] },
  { slug: "system-design", title: "System Design Path", description: "Use a repeatable design framework, then deepen APIs, caching, messaging, data distribution, and reliability.", targetRoles: ["backend_swe", "infrastructure_swe", "distributed_systems_engineer", "mid_level_swe"], difficulty: "intermediate-to-advanced", estimatedHours: 30, modules: ["system-design-interview-framework", "apis-requirements", "rate-limiting", "caching-strategies", "queues-workers", "replication-sharding", "observability", "reliability-backpressure"] },
  { slug: "quant-dev", title: "Quant Dev Path", description: "Connect C++, operating systems, networking, concurrency, performance, and market-data mechanics.", targetRoles: ["quant_developer", "hft_swe"], difficulty: "intermediate-to-advanced", estimatedHours: 38, modules: ["raii-resource-ownership", "move-semantics", "stl-containers-iterators", "processes-vs-threads", "tcp-vs-udp", "linux-for-quant-dev", "networking-for-quant-dev", "concurrency-for-quant-dev", "latency-cache-locality", "market-data-feeds", "order-books"] },
  { slug: "ai-efficient-engineer", title: "AI-Efficient Engineer Path", description: "Use coding agents with disciplined context selection, review, tests, and interview-appropriate boundaries.", targetRoles: ["backend_swe", "platform_engineer", "infrastructure_swe", "new_grad_swe"], difficulty: "beginner-to-intermediate", estimatedHours: 16, modules: ["good-ai-usage-principles", "token-efficient-prompting", "selecting-repo-context", "reviewing-ai-code", "hallucination-detection", "testing-verification"] },
];

export const learningPaths: LearningPathSeed[] = PATHS.map((path, pathIndex) => ({
  id: learningPathId(path.slug),
  slug: path.slug,
  title: path.title,
  description: path.description,
  targetRoles: path.targetRoles,
  difficulty: path.difficulty,
  estimatedHours: path.estimatedHours,
  order: pathIndex + 1,
  isPublished: true,
  modules: path.modules.map((moduleSlug, index) => ({
    moduleSlug,
    order: index + 1,
    isRequired: index < Math.ceil(path.modules.length * 0.75),
    label: index === 0 ? "warmup" : index >= Math.ceil(path.modules.length * 0.75) ? "advanced" : "core",
  })),
}));
